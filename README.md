Execução

1. Acesse o diretório do algoritmo
cd algorithm

2. Execute o algoritmo
./run.sh

3. Execute com uma instância específica
./run.sh ../instances/NOME_DA_INSTANCIA

Substitua `NOME_DA_INSTANCIA` pelo nome do arquivo da instância que deseja utilizar
